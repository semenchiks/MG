# Together - Совместный редактор кода

Веб-приложение для совместного редактирования HTML и CSS кода в реальном времени с отображением курсоров всех участников.

## 🚀 Особенности

### ✨ Совместное редактирование
- **Real-time синхронизация** - изменения отображаются мгновенно у всех участников
- **Yjs интеграция** - надежная система конфликт-резолюции
- **Monaco Editor** - профессиональный редактор кода с подсветкой синтаксиса

### 👥 Система курсоров
- **Живые курсоры** - видите курсоры всех пользователей в реальном времени
- **Персонализированные эмодзи** - каждый пользователь имеет уникальный смайлик
- **Плавная анимация** - курсоры двигаются с частотой 60fps
- **Отображение имен** - имя пользователя рядом с курсором

### 🎨 Интерфейс
- **Современный дизайн** - чистый и интуитивно понятный интерфейс
- **Адаптивная верстка** - работает на всех устройствах
- **Предварительный просмотр** - мгновенное отображение результата
- **Система доступности** - поддержка screen readers и навигации с клавиатуры

## 🛠 Технологии

### Backend
- **Node.js** - серверная часть
- **Express.js** - веб-фреймворк
- **Socket.IO** - WebSocket соединения
- **Yjs WebSocket Server** - синхронизация документов (порт 3001)

### Frontend
- **Vanilla JavaScript** - без фреймворков, чистый ES6+
- **Vite** - сборщик и dev-сервер
- **Monaco Editor** - редактор кода (VS Code engine)
- **Yjs** - CRDT для совместного редактирования
- **Y-Monaco** - интеграция Yjs с Monaco Editor

### Real-time синхронизация
- **Yjs Document** - структура данных для совместной работы
- **Yjs Awareness** - система отслеживания пользователей и курсоров
- **WebSocket Provider** - транспорт для синхронизации

## 📦 Установка и запуск

### Предварительные требования
- Node.js >= 16.0.0
- npm или yarn

### Локальная разработка

1. **Клонирование репозитория:**
```bash
git clone <repository-url>
cd together
```

2. **Установка зависимостей:**
```bash
npm install
```

3. **Запуск сервера:**
```bash
npm start
# или
node server.js
```

4. **Запуск dev-сервера (в отдельном терминале):**
```bash
npm run dev
```

5. **Открыть в браузере:**
```
http://localhost:5173
```

### Продакшн деплой

1. **Сборка проекта:**
```bash
npm run build
```

2. **Запуск продакшн сервера:**
```bash
npm start
```

## 🌐 Архитектура

### Серверы
- **HTTP сервер** (порт 3000) - Express.js для статических файлов и API
- **WebSocket сервер** (порт 3001) - Yjs WebSocket Provider для синхронизации

### Модули JavaScript

#### Основные компоненты
- `main.js` - точка входа приложения
- `app-initializer.js` - координатор инициализации всех модулей
- `socket-service.js` - управление WebSocket соединениями и Yjs
- `code-editor-manager.js` - управление редакторами HTML/CSS

#### Редактирование
- `code-editor.js` - обертка для Monaco Editor
- `editor-ui.js` - пользовательский интерфейс редактора

#### Совместная работа
- `cursor-overlay.js` - система отображения курсоров пользователей
- `auth-modal.js` - модальное окно авторизации

#### Утилиты
- `utils.js` - вспомогательные функции (включая генерацию эмодзи)
- `accessibility.js` - функции доступности

## 👥 Использование

### Подключение к сессии
1. Откройте приложение в браузере
2. Введите ваше имя в модальном окне
3. Начинайте редактировать код

### Совместная работа
- Все изменения синхронизируются автоматически
- Курсоры других пользователей отображаются в реальном времени
- Каждый пользователь имеет уникальный цвет и эмодзи

### Функции редактора
- **HTML редактор** - левая панель
- **CSS редактор** - правая панель
- **Предварительный просмотр** - центральная панель
- **Автосохранение** - все изменения сохраняются автоматически

## 🔧 Конфигурация

### Переменные окружения
```env
PORT=3000          # HTTP сервер
WS_PORT=3001       # WebSocket сервер
NODE_ENV=production # Режим работы
```

### Настройка для деплоя
Для деплоя на платформы типа Railway, Render:

1. Убедитесь, что `package.json` содержит:
```json
{
  "scripts": {
    "start": "node server.js",
    "build": "vite build",
    "dev": "vite"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
```

2. Настройте переменные окружения на хостинге

## 🎯 Особенности реализации

### Система курсоров
- Частота обновления: 60fps (16мс между обновлениями)
- Плавные CSS переходы с cubic-bezier анимацией
- Кеширование DOM элементов для оптимизации
- Автоматическое удаление неактивных курсоров

### Генерация эмодзи
Каждый пользователь получает уникальный эмодзи на основе хеша имени:
```javascript
const emojis = ['😀','😎','🐱','🦊','🐯','🐤','🦁','🐹','🐲','🐉'];
```

### Оптимизация производительности
- Дебаунсинг движения мыши
- Ограничение частоты логов в продакшене
- GPU-ускоренные трансформации для курсоров

## 🤝 Вклад в проект

1. Fork репозиторий
2. Создайте feature ветку (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📝 Лицензия

Этот проект распространяется под MIT лицензией. См. файл `LICENSE` для подробностей.

## 🔗 Полезные ссылки

- [Yjs Documentation](https://docs.yjs.dev/)
- [Monaco Editor API](https://microsoft.github.io/monaco-editor/)
- [Vite Documentation](https://vitejs.dev/)
- [Socket.IO Documentation](https://socket.io/docs/)

---

Сделано с ❤️ для совместной разработки